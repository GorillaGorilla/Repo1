module exports =  function (path, ext, function mf (err, list) {
	for (i = 0; i < list.length; i++){

		//var start = list[i].length - 3;
		var filList = [];
		var end = list[i].split('.');
		if (type == end[1]){
			console.log(list[i])
			filList.splice(0,0,list[i]

		}

		list = filList;
}
})